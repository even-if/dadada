<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"   
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.neu.mapper.UserMapper">
	<select id="getUserByUsernameAndPassword" parameterType="string" resultType="User">
		 select * from user where user_name=#{user_name}
	</select>
	<!-- 添加一条新记录,默认返回值是int，无法修改 -->  
    <insert id="addNewUser" parameterType="User" >  
        insert into user(user_name,user_pwd,user_phone,user_email,user_identity) values(#{user_name},#{user_pwd},#{user_phone},#{user_email},#{user_identity})  
    </insert> 
	
</mapper>